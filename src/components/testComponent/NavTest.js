import React from 'react'

 const NavTest = (props) => {
    debugger
    return (
        <div>
            {props.id}
            {props.login}
            {props.email} 
        </div>
    )
}

export default NavTest;